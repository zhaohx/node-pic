#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.npminstall/webpack-dev-server/1.15.0/webpack-dev-server/bin/webpack-dev-server.js" "$@"
  ret=$?
else 
  node  "$basedir/../.npminstall/webpack-dev-server/1.15.0/webpack-dev-server/bin/webpack-dev-server.js" "$@"
  ret=$?
fi
exit $ret
